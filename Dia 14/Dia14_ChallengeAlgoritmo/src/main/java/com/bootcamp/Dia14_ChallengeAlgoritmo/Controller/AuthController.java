package com.bootcamp.Dia14_ChallengeAlgoritmo.Controller;

import com.bootcamp.Dia14_ChallengeAlgoritmo.Request.LoginRequest;
import com.bootcamp.Dia14_ChallengeAlgoritmo.Respository.UserRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/users/auth")
public class AuthController {
/*        @Autowired
        UserRepository userRepository;

        @PostMapping("/login")
        public ResponseEntity<?> auth(/*@Valid*/ /*@RequestBody LoginRequest loginRequest){

        }
        @PostMapping("/register")
        public ResponseEntity<?> auth(){

}
*/
}
